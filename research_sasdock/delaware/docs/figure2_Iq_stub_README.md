# Figure 2 I(q) Stub

This is a **rough starter digitization** of Figure 2 from the Delaware sub-project paper set.

Files:
- `figure2_Iq_stub.csv`

Notes:
- Values are intentionally approximate (visual extraction from Figure 2 trend and markers).
- Units are preserved as shown in the figure: `q` in `A^-1`, `I(q)` in `cm^-1`.
- Use this only as a workflow/bootstrap stub until original numeric data are imported.
